{% extends 'layout.html.twig' %}
{% block title %}Liste - {{parent()}}{% endblock %}
{% block content %}
  <div class="container">
    {# {% if administrateurs %} #}
    <h1 class="display-1">Inscription ou Connexion</h1>
    <h2 clas="display-2">Pour les administrateurs</h2>

    {# <a href="{{ url('/administrateurs/create') }}" class="btn btn-primary m-4">Inscription</a> #}
    <a href="{{ url('/administrateurs/login') }}" class="btn btn-primary m-4">Connexion</a>
    <a href="/ReignsWebapp/public" class="btn btn-primary btn-lg">Revenir sur la page d'accueil</a>


{# 
    {% endif %} #}

  </div>

{% endblock %}
